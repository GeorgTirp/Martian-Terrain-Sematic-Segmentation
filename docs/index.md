# Martian Terrain Semantic Segmentation

Welcome! This site documents the Martian terrain semantic segmentation project,
including:

- An interactive **notebook**
- API documentation for the Python modules:
  `dataloader.py`, `models.py`, `distillation.py`,
  `explainability.py`, `optimizers.py`, `train_utils.py`,
  and `uncertainty.py`.

Use the navigation on the left to explore.
